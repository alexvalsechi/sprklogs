{
  "name": "@log-sparkui/desktop",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Electron shell for local Spark ZIP reduction + backend LLM analysis",
  "author": "SprkLogs Team",
  "main": "dist/main/index.js",
  "scripts": {
    "build:backend:win": "powershell -ExecutionPolicy Bypass -File ../../infra/scripts/build-python.ps1",
    "build": "tsc",
    "build:renderer": "esbuild renderer/features/spark-analyzer/SparkPlanViewer.jsx --bundle --outfile=renderer/features/spark-analyzer/spark-plan-viewer.js --platform=browser --target=chrome120 --minify",
    "start": "npm run build && npm run build:renderer && electron .",
    "check": "node -e \"require('./package.json'); console.log('desktop package ok')\"",
    "pack:win": "npm run build:backend:win && npm run build && npm run build:renderer && electron-builder --win --dir",
    "pack:win:debug": "npm run build:backend:win && npm run build && npm run build:renderer && electron-builder --win --dir -c.win.signAndEditExecutable=false",
    "dist:win": "npm run build:backend:win && npm run build && npm run build:renderer && electron-builder --win nsis --publish never",
    "dist:win:local": "npm run build:backend:win && npm run build && npm run build:renderer && electron-builder --win nsis --publish never -c.win.signAndEditExecutable=false"
  },
  "dependencies": {
    "@log-sparkui/ipc-types": "*",
    "posthog-node": "^4.18.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "electron": "31.7.7",
    "electron-builder": "^24.13.3",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.5"
  }
}
